.vercel
.scratch/

# --- Never commit client/PII exports (CardOpz reports, member/player data) ---
*.xlsx
*.xls

# --- Working backups / pipeline snapshots (local only) ---
*.bak
*_bak
*.audit_bak

# --- Python build artifacts ---
__pycache__/
*.pyc

# --- Scrape-source tooling kept local (avoid exposing data sources) ---
# (remove this block if you decide to publish the scrapers)
scrapers/

# --- Master/source data with full provenance — keep LOCAL, publish only the
#     sanitized casinos_social_clubs.geojson the map actually uses ---
geodata/master_venues.csv
geodata/master_venues_meta.json
geodata/geocodio_*.csv
